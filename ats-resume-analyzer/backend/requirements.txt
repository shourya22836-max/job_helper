fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12
pdfplumber==0.11.4
python-docx==1.1.2
pytesseract==0.3.13
Pillow==10.4.0
openai==1.51.0
httpx==0.27.2
python-dotenv==1.0.1
pydantic==2.9.2

# LiveKit — voice room infrastructure (token minting + room API)
livekit==0.12.0
livekit-api>=1.0.0

# LiveKit Agents — runs the realtime voice pipeline (STT, TTS, VAD, turn-taking)
# Plugins: Deepgram (STT), Cartesia (TTS), Silero (VAD), OpenAI LLM adapter
livekit-agents[deepgram,openai]~=1.2
livekit-plugins-cartesia~=1.2
livekit-plugins-silero~=1.2
livekit-plugins-turn-detector~=1.2
livekit-plugins-noise-cancellation~=0.2

# LangGraph + LangChain — drives the interview state machine, exposed to
# LiveKit Agents through `livekit.plugins.langchain.LLMAdapter`.
livekit-plugins-langchain~=1.1
langgraph>=0.2
langchain-core>=0.3
langchain-openai>=0.3
